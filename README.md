# PDF to Audiobook Converter

Simple Tkinter app to convert PDF text into an MP3 audiobook using PyMuPDF and gTTS.

## Setup
1. Create virtualenv:
```bash
python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
